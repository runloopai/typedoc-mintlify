# Save exact versions in package.json
save-exact=true

# Always use package-lock.json
package-lock=true

# Audit level for security checks
audit-level=moderate

# Only install production dependencies when NODE_ENV is production
production=false

